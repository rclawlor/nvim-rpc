use super::Buffer;


/// The below implementations are autogenerated using the Neovim API
impl Buffer {
    {{#each this}}
    /// Since: {{since}}
    pub fn {{name}}(&self{{#each parameters}}, {{this.name}}: {{{this.parameter_type}}}{{/each}}) {{#if return_type}}-> {{{return_type}}} {{/if}}{

    }
    {{/each}}
}

